# HKSC - 智能微服务电商平台 (JDK 21 重构版)

## 项目概述
基于 Spring Cloud Alibaba 2023 (Spring Boot 3) 的高并发分布式电商系统，全面适配 JDK 21 虚拟线程特性，集成 AI 智能推荐。

**开发周期**: 14天 | **团队**: 2人后端 + AI前端 | **模式**: 敏捷迭代 (双日验收制)

---

## 技术栈 (全新升级)

### 后端框架
- **开发语言**: JDK 21 (利用 Virtual Threads 提升高并发吞吐量)
- **核心框架**: Spring Boot 3.2.x
- **微服务**: Spring Cloud 2023 + Spring Cloud Alibaba 2023.0.1.0
- **服务治理**: Nacos 2.x (注册/配置中心)
- **流量防护**: Sentinel (限流降级)
- **远程调用**: OpenFeign (集成 LoadBalancer)
- **网关**: Spring Cloud Gateway
- **数据库**: MySQL 8.0 + MyBatis-Plus (Spring Boot 3版)
- **缓存**: Redis 7.x (缓存 + 分布式锁)
- **消息队列**: RabbitMQ (订单异步、削峰)
- **搜索引擎**: Elasticsearch 8.x (商品搜索)
- **AI集成**: 智能推荐算法 + LLM API

### 开发工具
- IntelliJ IDEA 2023+
- Maven 3.9+
- Docker + Docker Compose (一键部署)
- Postman (API测试)

---

## 项目架构

```text
hksc/                          # 父工程
├── hksc-gateway               # 网关服务 (端口: 8080)
├── hksc-auth                  # 认证中心 (端口: 8081)
├── hksc-user                  # 用户服务 (端口: 8082) - 开发者A
├── hksc-product               # 商品服务 (端口: 8083) - 开发者B
├── hksc-order                 # 订单服务 (端口: 8084) - 开发者A
├── hksc-cart                  # 购物车服务 (端口: 8085) - 开发者B
├── hksc-ai                    # AI推荐服务 (端口: 8086) - 开发者A
├── hksc-search                # 搜索服务 (端口: 8087) - 开发者B
├── hksc-common                # 公共模块 (工具类、实体、Result封装)
└── hksc-admin                 # 后台管理 (可选)
```

### 微服务职责划分

| 服务 | 负责人 | 核心功能 | 技术亮点 |
|------|--------|----------|----------|
| **基础设施** | 共同完成 (Day 1-2) | Gateway + Nacos + Auth | JDK21环境搭建、统一认证 |
| **用户服务** | 开发者A | 注册/登录、个人中心、地址 | JWT、Redis缓存 |
| **商品服务** | 开发者B | 商品CRUD、分类、库存 | 读写分离、缓存穿透 |
| **订单服务** | 开发者A | 下单、支付回调、状态机 | RabbitMQ、分布式事务 |
| **购物车** | 开发者B | 购物车增删改查 | Redis Hash、数据同步 |
| **AI推荐** | 开发者A | 协同过滤、商品推荐 | 虚拟线程并发调用、AI集成 |
| **搜索服务** | 开发者B | ES全文搜索、自动补全 | Elasticsearch 8.x Client |

---

## 14天开发与测试计划

**测试策略**: 采用“双日迭代”模式，**每两天进行一次阶段性联调测试（晚上执行）**，确保功能模块及时闭环。

### 阶段一: 基础搭建 (Day 1-2)
**目标**: 完成 JDK 21 基础架构与容器环境

- [ ] 搭建父工程 (依赖版本锁定) + Common模块
- [ ] 配置 Nacos 注册中心 (Docker部署)
- [ ] 搭建 Gateway 网关 (适配 Boot 3)
- [ ] 搭建 Auth 认证中心 (JWT生成/验证)
- [ ] 数据库设计 (用户、商品、订单核心表)
- [ ] 搭建 Redis + RabbitMQ + MySQL 环境 (Docker Compose)

> **🔴 Day 2 晚间验收点 (基础设施)**
> - [ ] Nacos 控制台服务列表正常显示
> - [ ] Gateway 网关可成功转发请求
> - [ ] Auth 服务能正常颁发 Token
> - [ ] 所有 Docker 容器运行正常

---

### 阶段二: 核心服务开发 (Day 3-8)

#### 第一轮迭代: 用户与商品 (Day 3-4)
**开发者A (用户)**
- [ ] 用户注册/登录 (手机号 + 验证码)
- [ ] JWT集成、登录态缓存 (Redis)
- [ ] 接口: `/api/user/register`, `/login`

**开发者B (商品)**
- [ ] 商品分类管理 (树形结构)
- [ ] 商品CRUD、上下架
- [ ] 接口: `/api/product/list`, `/detail/{id}`

> **🔴 Day 4 晚间验收点 (用户&商品)**
> - [ ] **接口联调**: Postman模拟登录获取Token -> 携带Token访问商品列表 -> 成功返回
> - [ ] **兼容性**: 确保 Jakarta EE 包名替换正确 (javax -> jakarta)

#### 第二轮迭代: 购物车与订单核心 (Day 5-6)
**开发者A (订单)**
- [ ] 创建订单 (库存扣减 + RabbitMQ)
- [ ] 订单状态机 (待付款→已付款→已发货)
- [ ] 接口: `/api/order/create`, `/list`

**开发者B (购物车)**
- [ ] 购物车增删改查 (Redis Hash存储)
- [ ] 库存管理 (Redis分布式锁防超卖)
- [ ] 接口: `/api/cart/add`, `/list`

> **🔴 Day 6 晚间验收点 (交易链路)**
> - [ ] **流程联调**: 登录 -> 加购 -> 购物车列表 -> 创建订单
> - [ ] **一致性验证**: 下单成功后，DB库存减少，购物车对应商品清除

#### 第三轮迭代: 搜索与AI (Day 7-8)
**开发者A (AI/支付)**
- [ ] 订单超时自动取消 (延迟队列)
- [ ] AI推荐接口 (协同过滤)
- [ ] 集成LLM API生成商品描述 (利用虚拟线程高并发请求)

**开发者B (搜索)**
- [ ] ES索引设计 + 数据同步
- [ ] 全文搜索、搜索自动补全

> **🔴 Day 8 晚间验收点 (智能与搜索)**
> - [ ] **功能闭环**: 搜索商品(ES) -> 查看AI描述 -> 下单 -> 模拟支付 -> 订单状态变更

---

### 阶段三: 高并发优化 (Day 9-10)
**共同完成**
- [ ] Sentinel 限流降级配置
- [ ] Redis 缓存预热、穿透/雪崩处理
- [ ] 热点商品秒杀接口 (库存预减 + MQ异步)
- [ ] 接口幂等性处理

> **🔴 Day 10 晚间验收点 (性能与稳定性)**
> - [ ] **压测验证**: JMeter压测商品详情页 (验证Redis缓存命中)
> - [ ] **限流验证**: 快速刷新接口触发 Sentinel 限流

---

### 阶段四: 全链路集成测试 (Day 11-12)
**目标**: 全量回归测试，Bug修复，准备上线

- [ ] **全链路回归**: 注册 -> 登录 -> 搜索 -> 详情 -> 加购 -> 下单 -> 支付 -> 个人中心
- [ ] **异常测试**: 服务宕机降级、MQ消息堆积、分布式锁并发测试

> **🔴 Day 12 晚间验收点 (预上线封版)**
> - [ ] 核心接口 (P0级) 成功率 100%
> - [ ] 代码合并至 `main` 分支

---

### 阶段五: 部署上线 (Day 13-14)
- [ ] 编写 Dockerfile (适配 OpenJDK 21)
- [ ] Docker Compose 一键部署
- [ ] 云服务器部署

> **🔴 Day 14 晚间验收点 (最终交付)**
> - [ ] 线上公网地址可访问
> - [ ] Postman 接口文档完整

---

## 快速启动

### 1. 环境准备
```bash
# 克隆项目
git clone https://github.com/yourname/hksc.git
cd hksc

# 启动基础设施 (MySQL, Redis, Nacos, RabbitMQ, ES)
docker-compose up -d
```

### 2. 启动微服务
**注意**: 请确保本地安装 JDK 21
```bash
# 编译安装公共模块
cd hksc-common && mvn clean install

# 启动服务
cd hksc-gateway && mvn spring-boot:run
# ... 其他服务
```

---

## 数据库设计 (核心表)

```sql
-- 用户表
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `phone` VARCHAR(11) UNIQUE NOT NULL,
  `password` VARCHAR(64) NOT NULL,
  -- 其他字段...
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 其他表结构保持一致...
```

## 注意事项
1. **JDK 版本**: 必须使用 JDK 21+
2. **包名变更**: `javax.*` 已变更为 `jakarta.*` (如 `jakarta.servlet`)
3. **MyBatis Plus**: 需使用 `mybatis-plus-spring-boot3-starter`
```

