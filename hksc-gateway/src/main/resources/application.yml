server:
  port: 8080 # 网关一般占用 8080 或 80

spring:
  application:
    name: hksc-gateway
  profiles:
    active: dev

  # 👇 Spring Boot 3.x 连接 Nacos 必须加这一段，否则无法读取配置中心
  config:
    import:
      - optional:nacos:${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}

  cloud:
    nacos:
      server-addr: localhost:8848 # Nacos 地址
      discovery:
        server-addr: ${spring.cloud.nacos.server-addr}
      config:
        server-addr: ${spring.cloud.nacos.server-addr}
        file-extension: yaml
        # 共享配置 (引入通用配置)
        shared-configs:
          - data-id: hksc-common.yaml
            refresh: true
    sentinel:
      transport:
        dashboard: localhost:8858
        port: 8719
        client-ip: 127.0.0.1

    # 网关路由配置
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # 1. Auth 服务
        - id: auth_route
          uri: lb://hksc-auth
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=1

        # 2. User 服务
        - id: user_route
          uri: lb://hksc-user
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=1

        # 3. Product 服务
        - id: product_route
          uri: lb://hksc-product
          predicates:
            - Path=/api/product/**
          filters:
            - StripPrefix=1

        # 4. Order 服务
        - id: order_route
          uri: lb://hksc-order
          predicates:
            - Path=/api/order/**
          filters:
            - StripPrefix=1

        # 5. Cart 服务
        - id: cart_route
          uri: lb://hksc-cart
          predicates:
            - Path=/api/cart/**
          filters:
            - StripPrefix=1

        # 6. Search 服务
        - id: search_route
          uri: lb://hksc-search
          predicates:
            - Path=/api/search/**
          filters:
            - StripPrefix=1

        # 7. AI 服务 (已修复缩进)
        - id: ai-route
          uri: lb://hksc-ai
          predicates:
            - Path=/api/ai/**
          filters:
            - StripPrefix=1