spring:
  application:
    name: hksc-cart
  profiles:
    active: dev
  config:
    import:
      - optional:nacos:hksc-common.yaml
      - optional:nacos:${spring.application.name}-${spring.profiles.active}.yaml
  cloud:
    nacos:
      server-addr: localhost:8848