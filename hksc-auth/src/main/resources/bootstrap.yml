# 文件路径: hksc-auth/src/main/resources/bootstrap.yml (精简版)

spring:
  application:
    name: hksc-auth # 服务名，注册到 Nacos 时使用

  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 # Nacos 服务器地址
      discovery:
        namespace: ${nacos.namespace:public} # 命名空间ID

    # 【删除或注释掉】原有的 server: port 和 threads: virtual 配置
    # server:
    #   port: 8081
    # threads:
    #   virtual:
    #     enabled: true